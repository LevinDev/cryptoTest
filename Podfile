# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BitTicker' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BitTicker
  # Pods used for Firebase sign up and sign.
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  
  # Hud
  pod 'PKHUD', '~> 5.0'

end
